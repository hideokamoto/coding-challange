/* eslint-disable no-use-before-define */
// import { WpPageActionTypes } from '../../actions/types/wp/pages'

export function departure (state = {}, action) {
  switch (action.type) {
    default:
      return state
  }
}
